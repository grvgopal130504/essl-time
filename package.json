{
  "name": "essl-k90-pro-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "eSSL K90 Pro -> local Node.js ADMS receiver + React Vite live dashboard",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix frontend run build",
    "setup": "npm run install:all && npm run build && npm run migrate",
    "serve": "npm --prefix backend start",
    "migrate": "npm --prefix backend run migrate",
    "backend": "npm --prefix backend run dev",
    "frontend": "npm --prefix frontend run dev",
    "simulate": "npm --prefix backend run simulate",
    "test:timesheet": "npm --prefix backend run test:timesheet",
    "test": "npm --prefix backend run test:timesheet && npm --prefix backend run test:absence && npm --prefix backend run test:feed-reset && npm --prefix backend run test:feed-rollover && npm --prefix frontend run test:view && npm --prefix frontend run test:feed"
  }
}
